# gloabally install the latest verions of eslint and the google config
# at the moment eslint 3.13 and google-config 0.7
npm i -g eslint eslint-config-google

# check
npm -g ls eslint eslint-config-google
